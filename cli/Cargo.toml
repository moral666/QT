[package]
name = "secure_messenger_cli"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Demo de terminal: liga core + transport + server numa conversa real, visivel"
publish = false

[lib]
name = "secure_messenger_cli"
path = "src/lib.rs"

[dependencies]
secure_messenger_core = { path = "../core" }
secure_messenger_transport = { path = "../transport" }
secure_messenger_server = { path = "../server" }
secure_messenger_storage = { path = "../storage" }
x25519-dalek = { version = "2", features = ["static_secrets"] }
ed25519-dalek = { version = "2" }
tokio = { version = "1", features = ["full"] }
clap = { version = "4", features = ["derive"] }
